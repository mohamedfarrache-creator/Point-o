name: point_o
description: A local-first attendance, shift and overtime tracker.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  mobile_scanner: ^5.2.3
  path: ^1.9.0
  provider: ^6.1.2
  sqflite: ^2.3.3+1
  table_calendar: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
